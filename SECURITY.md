# Security Policy / 安全说明

## Supported version

当前仅维护 `VERSION` 中列出的最新版本。安全修复不会保证回移到旧发布包。

## Security model

本项目设计为单用户、本机运行的新闻工具：

- HTTP 服务只绑定 `127.0.0.1`。
- 不提供公网部署、多人账户、远程登录或局域网共享。
- 不接收交易所 API Key，不访问持仓和资金。
- 新闻聚合无需凭据。
- AI Key 由用户在浏览器中输入，并保存在该浏览器的站点本地存储。
- Key 只通过本机代理转发给用户选择的 AI 服务商。

## Proxy protections

本地代理采取以下限制：

- `/p` 只接受来自本应用页面的同源请求。
- 目标必须使用 HTTPS。
- DNS 解析结果必须是公网地址。
- 重定向目标会再次检查。
- 请求正文上限 2 MiB，上游响应上限 12 MiB。
- 只转发必要的鉴权与内容类型请求头。
- 日志只记录目标主机名，不记录完整 URL、Key 或正文。
- HTML 响应设置 CSP、`X-Frame-Options`、`nosniff` 和权限限制。

这些措施降低风险，但本程序不是通用安全网关。不要将监听地址改为 `0.0.0.0` 后暴露给局域网或互联网。

## Credential guidance

- 只使用服务商官方控制台创建的 Key。
- 为本程序单独创建、设置合理额度和权限的 Key。
- 不要使用具有组织管理权限的高权限 Key。
- 不要把 Key 写入源码、Issue、截图、日志或提交记录。
- 公共电脑使用后点击“清除全部本地数据”。
- 怀疑泄露时立即在服务商后台撤销并轮换 Key。

## Known limitations

- `localStorage` 不是硬件安全模块；能以同一系统用户控制浏览器配置的人可能读取其中的数据。
- 本项目使用内联脚本和内联样式，因此 CSP 需要允许 `'unsafe-inline'`。
- 第三方新闻源内容和可用性不受本项目控制。
- 自定义 AI 兼容接口由用户选择，本项目无法保证其隐私政策和安全性。
- 如果系统本身、浏览器扩展或根证书环境已被入侵，本项目不能保护 Key。

## Reporting a vulnerability

请不要在公开 Issue 中披露仍可利用的漏洞、Key、个人信息或复现数据。

发布到 GitHub 后，优先通过仓库的 **Security → Report a vulnerability** 私密报告，并包含：

- 受影响版本。
- 影响范围。
- 最小复现步骤。
- 是否需要特定浏览器、系统或服务商。
- 已做过的安全处理。

维护者确认并发布修复前，请给予合理处理时间。
