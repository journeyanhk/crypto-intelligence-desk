# 币圈新闻监控台 · 完整使用说明

本文面向第一次接触本程序的用户，从下载、启动、AI 配置、界面使用、数据安全到常见故障逐项说明。

## 一、程序能做什么

本程序是本地运行的加密行业新闻情报面板，主要功能包括：

1. 定时读取多个公开新闻源。
2. 合并并去除重复标题。
3. 按新闻时间展示实时情报流。
4. 使用用户自选的 AI 服务分析每条新闻。
5. 把影响等级 4–5 的重大新闻提升到顶部和右侧重大情报队列。
6. 提供新闻等级、方向和关键词筛选。
7. 提供宏观事件倒计时、声音提醒、日间/夜间模式。

本程序不包含以下功能：

- 实时币价、K 线或盘口。
- 交易策略、回测或收益展示。
- 模拟盘或实盘下单。
- 持仓、资金、交易所账户管理。
- 交易所 API Key 接入。

## 二、运行条件

### Windows 10 / 11

推荐直接使用发布包自带的 `start.bat`。如果电脑没有 Python，程序会在首次运行时下载官方便携版 Python 3.12.10。

首次运行需要：

- 能访问 `www.python.org` 的网络，用于下载约 11 MB 的便携运行环境。
- 能访问所启用的新闻源。
- 如果使用 AI，还需要能访问所选 AI 服务商。
- 大约 30 MB 可用磁盘空间。

便携 Python：

- 只放在程序目录的 `.runtime` 文件夹。
- 不安装到 Windows。
- 不修改注册表和系统 PATH。
- 不需要管理员权限。
- 下载完成后会核对官方 SHA-256；校验不通过不会安装。

如果电脑已经安装 Python 3.8 或更高版本，启动器会优先使用现有 Python，不再下载便携环境。

### macOS / Linux

需要预先安装 Python 3.8 或更高版本。程序不依赖 pip 包，只使用 Python 标准库。

## 三、下载与首次启动

### Windows

1. 下载 ZIP 发布包。
2. 右键 ZIP，选择“全部解压”。不要直接在压缩包预览窗口中运行文件。
3. 将文件夹放在有写入权限的位置，例如桌面、文档或其他数据盘。
4. 双击 `启动监控台.bat`。
5. 如果电脑没有 Python，等待首次下载和校验完成。
6. 默认浏览器会打开 `http://127.0.0.1:8899`。
7. 不要关闭启动窗口；它就是本地服务进程。

如果 Windows SmartScreen 提示“已保护你的电脑”，请先确认文件来自可信的项目发布页，并核对发布包校验值。确认无误后再选择“更多信息 → 仍要运行”。不要对来源不明的副本这样操作。

### macOS / Linux

在终端进入程序目录：

```sh
chmod +x start.sh
./start.sh
```

如果浏览器没有自动打开，请手动访问：

```text
http://127.0.0.1:8899
```

### 停止程序

- Windows：关闭启动窗口，或在窗口中按 `Ctrl+C`。
- macOS / Linux：在运行终端中按 `Ctrl+C`。
- 关闭网页标签不会停止后台本地服务。

## 四、首次进入页面

页面打开后会立刻拉取已启用的新闻源。第一次同步会加载一批已有新闻，为避免瞬间产生大量 AI 请求，首次只分析最新 5 条；之后每轮出现的新新闻都会进入分析队列。

没有配置 AI Key 时：

- 新闻聚合、筛选、宏观日历和主题切换仍然可用。
- 新闻卡片的 AI 区域会提示“未配置 AI Key”。
- 不会向任何 AI 服务商发送请求。

## 五、配置 AI

### 1. 选择服务商

点击右上角“设置”，在“AI 分析模型”中选择：

- DeepSeek
- Claude / Anthropic
- OpenAI
- Gemini
- Grok / xAI
- 自定义 OpenAI 兼容接口

程序不提供共享 Key。你需要从对应服务商的官方控制台自行创建 API Key，并自行承担服务商可能产生的调用费用。

### 2. 输入 Key 与自动匹配模型

1. 选择服务商。
2. 在密码输入框中粘贴 API Key。
3. 程序会调用该服务商的模型列表接口。
4. 如果当前模型仍然有效，会保留当前模型。
5. 否则会按文本/对话模型优先级选择一个可用模型。
6. 你仍可从建议列表选择或手动输入模型名。

自动检测失败不一定表示 Key 无效。有些兼容接口不提供 `/models`，此时可以手动填写模型名。

### 3. 自定义 OpenAI 兼容接口

需要填写：

- 接口地址，例如 `https://api.example.com/v1`
- API Key
- 模型名

安全限制：

- 只允许 HTTPS。
- 只允许可公开解析的公网地址。
- `localhost`、`127.0.0.1`、局域网 IP、链路本地地址和其他非公网目标会被本地代理拒绝。

### 4. 双层分析

开启后，首次模型评为 4–5 级的重大新闻可以交给第二个服务商复核。第二个服务商也必须事先保存有效 Key。

双层分析会增加请求次数和费用，建议先了解所用服务商的计费规则。

## 六、API Key 保存与清除

### 保存位置

Key 保存在浏览器 `localStorage` 中，存储空间属于当前站点来源：

```text
http://127.0.0.1:8899
```

发布版使用独立键名 `crypto_intel_oss_v1_settings`，不会读取原开发版本使用过的存储键。

Key 不会写入：

- `index.html`
- `proxy.py`
- 启动脚本
- `.env` 文件
- 日志文件
- 发布压缩包

### 清除方法

打开“设置”，点击底部“清除全部本地数据”。确认后会删除：

- 所有已保存的 AI Key。
- 服务商、模型、主题和轮询设置。
- 自定义数据源地址。
- 自定义宏观事件。

清除浏览器站点数据或更换浏览器也会失去这些设置。

### 公共电脑注意事项

不要在公共电脑、网吧、共享账户或远程临时环境中长期保存 Key。使用完成后主动清除本地数据，并到 AI 服务商后台撤销不再使用的 Key。

## 七、界面说明

### 顶部状态栏

- “实时聚合”：表示页面处于工作状态。
- “同步时间”：显示最近一次新闻轮询完成时间。
- “宏观日历”：打开内置和自定义宏观事件。
- “声音开启/关闭”：切换重大新闻声音提醒。
- “THEME”：在日间与夜间模式之间切换。
- “设置”：打开 AI、新闻源和高级设置。

### 左栏

- 影响级别：全部、≥2、≥3、≥4。
- 影响方向：不限、正面、负面。
- 关键词：搜索标题与正文。
- 信源网络：绿色表示最近请求成功，红色表示当前轮询失败。

### 中间情报流

- 每条新闻都有独立边框。
- 新闻正文和 AI 分析使用不同底色。
- 后续轮询出现的新消息会从顶部弹入。
- 如果系统开启“减少动态效果”，入场动画会自动停用。
- 点击标题或正文可展开/收起较长内容。
- 点击“原文”会打开新闻源页面。

### 右栏

- 重大情报：显示 AI 评为 4–5 级的新闻。
- 宏观窗口：显示最近事件倒计时。
- 系统状态：显示 AI 是否配置、当前模型、轮询周期和双层复核状态。

窄屏设备会自动收起右栏并调整筛选布局。

## 八、AI 分析字段

每条已分析新闻可能显示：

- 方向：正面、负面或中性。
- 短期影响：小时/天级。
- 长期影响：周/月级。
- 影响级别：L1–L5。
- 置信度：0–100%。
- 分类：监管、ETF、宏观、安全、机构、技术、生态等。
- 受影响资产代码。
- “或已反映”：模型认为市场可能提前消化。
- 核心判断：模型给出的因果逻辑。
- 解读失效条件：可能推翻当前判断的条件。

### JSON 自动重试

如果 AI 没有返回 JSON，或返回的 JSON 无法解析，程序会：

1. 显示自动重试进度。
2. 最多额外重试 3 次。
3. 仍失败后显示错误和手动“重试”入口。

HTTP 401、Key 无效、余额不足、限流、模型无权限等错误不会按 JSON 格式错误重复请求，避免无意义消耗接口额度。

## 九、新闻源设置

默认启用的来源包括 PANews、币安公告、Odaily、吴说、深潮和链捕手；金色财经、BlockBeats 可在设置中按需开启。具体可用性取决于第三方站点。

新闻源可能发生：

- 接口地址调整。
- 返回字段变更。
- 地区限制。
- WAF 或反爬验证。
- 临时限流、超时或维护。
- RSS 内容为空。

高级设置中的地址覆盖功能适合接口迁移时临时使用。它不能让任意网页自动变成可解析新闻源；返回格式仍需兼容现有解析器。

## 十、主题、轮询与声音

### 主题

右上角可即时切换，也可在设置中明确选择。主题偏好保存在浏览器中。

### 轮询间隔

默认 30 秒，允许范围 10–600 秒。间隔过短会增加新闻站点请求压力，也更容易触发限流。

### 声音提醒

浏览器通常要求用户先点击过页面，网页才能播放声音。首次打开后可点击页面任意位置，再到设置中使用“试听”。

## 十一、宏观日历

程序包含一组默认宏观事件，也支持手动添加和删除事件。时间使用本机浏览器时区显示。

自定义事件保存在当前浏览器中。清除全部本地数据时会一并恢复默认。

宏观事件时间可能因官方日程调整而变化，使用前应核对对应机构的最新公告。

## 十二、网络与安全边界

### 本地服务

`proxy.py` 只监听：

```text
127.0.0.1:8899
```

其他局域网设备无法直接访问。程序不会主动创建公网服务，也不需要路由器端口映射。

### 本地代理

浏览器受跨域规则限制，因此页面把新闻和 AI 请求先发给同一目录中的本机代理。代理随后连接目标 HTTPS 服务。

代理限制包括：

- 只接受来自监控台页面的浏览器请求。
- 只允许公网 HTTPS 目标。
- 重定向后仍会重新检查目标。
- 限制请求正文和上游响应大小。
- 不记录 Key、请求正文和完整 URL。

### 页面保护

本地服务会发送 CSP、禁止 iframe 嵌入、禁止 MIME 猜测和权限限制等响应头。新闻字段经过 HTML 转义；新闻链接只接受 HTTP/HTTPS。

更详细内容见 `SECURITY.md`。

## 十三、常见问题

### 1. 双击后一闪而过

请不要从压缩包内部直接运行。先完整解压，再运行 `start.bat`。如果仍失败，在文件夹空白处打开终端并运行：

```powershell
.\start.bat
```

这样可以看到具体提示。

如果窗口出现乱码，并连续提示某些文字“不是内部或外部命令”，说明使用的是受部分 Windows 批处理编码影响的 1.0.0 包。请改用 1.0.1 或更高版本；新版批处理入口为纯 ASCII + CRLF，实际启动逻辑已移入 PowerShell 文件。

### 2. 便携运行环境下载失败

检查：

- 是否能访问 `https://www.python.org/`。
- Windows 系统日期和时间是否正确。
- 公司代理、防火墙或安全软件是否阻止 PowerShell 下载。
- 磁盘是否有写入权限。

也可以从 Python 官网自行安装 Python 3.8+，然后重新运行。

### 3. 提示校验失败

启动器会把下载文件的 SHA-256 与内置官方值比较。校验失败时不会安装。请删除未完成的临时下载、检查网络代理后重试，不要绕过校验。

### 4. 8899 端口被占用

先检查是否已经打开过本程序。访问：

```text
http://127.0.0.1:8899/ping
```

如果不是本程序占用，可关闭对应程序，或手动指定端口：

```powershell
$env:CID_PORT='8900'
python .\proxy.py
```

随后在同一个终端中运行 `start.bat`；新版启动器会读取 `CID_PORT` 并自动打开对应端口。

### 5. 某个新闻源持续红灯

通常是第三方接口变化或网络限制，不代表整个程序损坏。可以暂时关闭该源，查看项目更新，或在高级设置中填写新的兼容地址。

### 6. 页面没有新闻

检查：

- 至少启用了一个新闻源。
- 左侧筛选是否过严。
- 浏览器开发者工具的 Network 中 `/p` 是否返回错误。
- 启动窗口是否仍然打开。
- 系统网络和 DNS 是否正常。

### 7. Key 正确但模型检测失败

可能原因：

- 服务商没有开放模型列表接口。
- Key 没有读取模型列表权限。
- 自定义 Base URL 少了 `/v1` 或路径不兼容。
- 服务商临时异常。

可以参考服务商官方控制台手动填写模型名。

### 8. AI 分析一直失败

根据错误分别检查：

- 401/403：Key、项目或模型权限。
- 404：接口地址或模型名。
- 429：频率限制、额度或余额。
- 非 JSON：模型没有遵守输出格式；程序会自动重试 3 次。
- upstream request failed：DNS、TLS、代理、防火墙或目标服务暂时不可达。

### 9. 声音不响

先点击一次网页，再打开声音并“试听”；同时检查浏览器标签是否静音、系统输出设备和音量设置。

### 10. 日间/夜间设置没保存

浏览器可能处于隐私模式、禁用了本地存储，或关闭时自动清理站点数据。

## 十四、更新与迁移

建议升级方式：

1. 下载并解压新版本到新文件夹。
2. 关闭旧版本。
3. 启动新版本。
4. 重新检查服务商、模型和新闻源。

浏览器数据按“协议 + 主机 + 端口”归属。如果新旧版本仍使用 `http://127.0.0.1:8899` 且存储键不变，它们可能共享设置。此开源发布版使用独立命名空间，不会读取早期开发版本的 `cnt_set` / `cnt_ev`。

不要把 `.runtime`、浏览器配置目录或个人日志放进公开发布包。

## 十五、发布前自检

项目维护者在 PowerShell 中运行：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\check-release.ps1
```

检查器会确认：

- 必需文件齐全。
- 没有常见 API Key、JWT 或私钥块。
- 没有指向维护者 Windows 用户目录的个人绝对路径。
- 没有旧版浏览器存储键。
- 没有 `.runtime`、`__pycache__` 等本机产物。

检查器只报告规则名称和文件名，不输出疑似秘密内容。

## 十六、重要免责声明

AI 评分是自动生成的概率性判断，不是事实裁决，也不是投资信号。新闻摘要可能缺失上下文，第三方接口也可能返回旧闻、误报或重复内容。任何重大事件请打开原文，并用多个独立信息源交叉验证。

使用者应自行承担 AI API 费用、第三方服务条款合规、数据来源授权以及基于信息做出决策的风险。
