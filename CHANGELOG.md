# Changelog

## 1.0.1

- 修复部分全新 Windows 电脑执行 `start.bat` 时出现乱码和“不是内部或外部命令”的问题。
- 将批处理入口缩减为纯 ASCII、CRLF 格式，复杂启动逻辑迁移到 `tools/start.ps1`。
- 启动器支持读取 `CID_PORT`，并在确认本程序已成功监听后再打开浏览器。
- 增加启动器无副作用预检模式，用于发布验证。

## 1.0.0

- 首个开源发布版。
- 新闻聚合、AI 分析、自动模型匹配、重大新闻优先、日夜主题和 JSON 自动重试。
- Windows 便携 Python 首次启动支持。
